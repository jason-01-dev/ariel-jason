{
  "name": "ariel-jason",
  "private": true,
  "description": "Root shim so Vercel detects Next.js (app lives in web/)",
  "scripts": {
    "build": "cd web && npx prisma generate && npx next build",
    "dev": "cd web && npx next dev",
    "postinstall": "cd web && npm install && npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "next": "^15.2.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "prisma": "^6.5.0",
    "typescript": "^5"
  }
}
